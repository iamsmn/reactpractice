import React from "react";
import { Row, Col, Typography, Layout, Form } from "antd";
const { Title } = Typography;
const { Item } = Form;
const { Footer } = Layout;

const SmnFooter = props => (
  <Footer style={{ textAlign: "center" }}>Created By smn</Footer>
);
export default SmnFooter;
